<?xml version="1.0" encoding="utf-8"?>
<!-- The window behind the app while the process starts. It is the app's
     own background colour so a cold start does not flash white before
     the first frame. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />
</layer-list>
